package com.tofiq.mvi_imdb.presentation.base

/**
 * Base marker interface for all MVI Intents.
 * Intents represent user actions or events that trigger state changes.
 * 
 * Requirements: 8.1 - WHEN a user action occurs THEN the View SHALL emit an Intent to the ViewModel
 */
interface MviIntent
