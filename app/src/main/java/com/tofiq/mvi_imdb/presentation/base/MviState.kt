package com.tofiq.mvi_imdb.presentation.base

/**
 * Base marker interface for all MVI States.
 * States represent the immutable UI state that the View renders.
 * 
 * Requirements: 8.2 - WHEN the ViewModel receives an Intent THEN the ViewModel SHALL process it and emit a new immutable State
 */
interface MviState
